<% include ../partials/header %>

</head>
<body>

<% include ../partials/navbar %>
<div class="container">
  <br />
  <div class="row">
    <div class="col-md-5">
      <h3><%= foundEvent.name %></h3>
      <p class='mt-3'>Dates: <%= foundEvent.dates[0].toLocaleDateString("en-US") + " - " + foundEvent.dates[foundEvent.dates.length-1].toLocaleDateString("en-US") %></p>
    </div>
  </div>
</div>

<% include ../partials/footer %>
